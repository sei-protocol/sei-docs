import { AppCardsGridCategory } from '../../components';
import { Tag } from '../../data/appData';

# RPC Providers

RPC providers offer endpoints for developers to interact with the Sei
blockchain, archive nodes, genesis files, and more. Some notable providers
include:

<AppCardsGridCategory category="rpc%20provider" />
