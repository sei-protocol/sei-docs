---
title: "Cosmos API Endpoints | Sei Docs"
description: "Explore REST and gRPC endpoints for Cosmos, CosmWasm, IBC, and Sei-specific modules. Integrate Sei into your dApp easily."
keywords: ["sei docs", "api endpoints", "rest", "grpc", "cosmos sdk", "web3 docs"]
---

import openapi from '../../data/cosmos-openapi.json';
import { getUniqueSections } from '../../components/APIModule/utils';
import { APIModulePaths } from '../../components';

# API Endpoints

## cosmos

<APIModulePaths prefix='cosmos' basePaths={getUniqueSections(openapi.paths, ['cosmos'])} />

## cosmwasm

<APIModulePaths prefix={'cosmwasm'} basePaths={getUniqueSections(openapi.paths, ['cosmwasm'])} />

## ibc

<APIModulePaths prefix='ibc' basePaths={getUniqueSections(openapi.paths, ['ibc'])} />

## sei-protocol

<APIModulePaths prefix='sei-protocol' basePaths={getUniqueSections(openapi.paths, ['sei-protocol'])} />

## seichain

<APIModulePaths prefix='seichain' basePaths={getUniqueSections(openapi.paths, ['seichain'])} />
