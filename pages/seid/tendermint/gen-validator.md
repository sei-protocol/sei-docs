### `seid tendermint gen-validator`
```ansi
Generate new validator keypair

Usage:
  seid tendermint gen-validator [flags]

Flags:
  -h, --help         help for gen-validator
      --key string   Key type to generate privval file with. Options: ed25519, secp256k1 (default "ed25519")

Global Flags:
      --home string         directory for config and data (default "~/.sei")
      --log_format string   The logging format (json|plain)
      --log_level string    The logging level (trace|debug|info|warn|error|fatal|panic)
      --trace               print out full stack trace on errors

```